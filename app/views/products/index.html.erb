
<!-- PARA PODERLO CORRER TOCO QUITAR // = require jquery DE aplication.js -->
<!-- <div class='container'>
  <div class='row'>
    <div class='col-ms-12'>
      <h1>Productos</h1>
      <% if signed_in? %>
      <div class='text-right'>
        <%= link_to 'Publicar Producto', new_product_path, class: 'btn btn-success btn-lg' %>
      </div>
      <%end %>
      <br>
      <table class='table'>
        <tr>
          <th>Id</th>
          <th>Nombre</th>
          <th>URL</th>
          <th>Descripción</th>
          <th></th>
        </tr>
        <% @products.each do |p| %>
        <tr>
          <td><%= p.id %></td>
          <td><%= link_to p.name, p %></td>
          <td><%= p.url %></td> -->


          <!-- <td><%= link_to p.url, p.url %></td> Para generar link en el url-->


          <!-- <td><%= p.description %></td>
          <th>
            <% if signed_in? %>
              <%= link_to "Editar", edit_product_path(p), class: 'btn btn-primary btn-sm' %>
              <%= link_to "Eliminar", p, method: :delete, data:{confirm:"Estas seguro de eliminar este producto?"}, class: 'btn btn-danger btn-sm' %>
            <%end %>
          </th>
        </tr>
        <%end %>
      </table>
    </div>
  </div>
</div> -->

<div class="container">
  <div class="row">
    <div class="col-sm-10 col-sm-offset-1">

      <div class="products">
        <% @products.each do |product| %>
        <div class="product clearfix">
            <% if product.voted_by? current_user %>
              <%= link_to product_vote_path(product), method: :delete, class: "product-upvote upvote-active" do %>
              <span class="upvote-arrow"></span>
              <span class="upvote-count"><%= product.votes.count %></span>
              <% end %>
              <% else %>
              <%= link_to product_vote_path(product), method: :post, class: "product-upvote" do %>
              <span class="upvote-arrow"></span>
              <span class="upvote-count"><%= product.votes.count %></span>
            <% end %>
          <% end %>
          <div class="product-details">
            <span class="product-title"><%= link_to product.name, product %></span>
            <span class="product-description"><%= product.description %></span>
          </div>
          <div class="product-comments">
            <span class="glyphicon glyphicon-comment"></span>
            <span class="comment-count"><%= product.comments.count %></span>
          </div>
        </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
